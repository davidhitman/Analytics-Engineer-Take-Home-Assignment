version: 2

sources:
  - name: raw
    description: "Raw TeleClinic application tables"
    tables:
      - name: consultations
        description: "Consultation records with mixed timezone timestamps"
        columns:
          - name: consultation_id
            description: "Primary key"
          - name: patient_id
            description: "Patient identifier"
          - name: created_at
            description: "Consultation creation time (UTC)"
          - name: consultation_started_at
            description: "Consultation start time (mixed timezone formats)"
